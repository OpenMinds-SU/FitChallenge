@model IEnumerable<Fmi.OpenMinds.FitChallenge.Models.MuscleGroup>
@{
    ViewBag.Title = "Muscle Groups";
}

<h2>Muscle Groups</h2>
@*@Html.ActionLink("Create", "Create", "Measurement")*@
<table class="table table-striped table-hover table-bordered">
<thead>
<tr>
        <th>Actions</th>
        <th>Id</th>
        <th>Name</th>
    </tr>
</thead>
<tbody>
    @foreach(var muscleGroup in Model)
    { 
        <tr>
            <td>
          @*  @Html.ActionLink("Edit", "Edit", "Measurement", new { Id = measurement.Id }, new { }) |
            @Html.ActionLink("Delete", "Delete", "Measurement", new { Id = measurement.Id }, new { })*@
            </td>
            <td>@muscleGroup.Id</td>
            <td>@muscleGroup.Name</td>

        </tr>
    }
</tbody>
</table>